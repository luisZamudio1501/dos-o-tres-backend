package com.dosotres.prayer;

/**
 * Alcance de un pedido de oración.
 * PRIVATE: sin grupo, solo visible para el autor (espacio personal).
 * GROUP: compartido con un grupo (group_id seteado).
 */
public enum PrayerVisibility {
    PRIVATE,
    GROUP
}
