package com.dosotres.goal;

public enum GoalMode {
    /** Ora cuando quiere; la meta se cumple al alcanzar los minutos del día. */
    FREE,
    /** Igual que FREE, pero con una hora a la que la app recuerda por push. */
    SCHEDULED
}
