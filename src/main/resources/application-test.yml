spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dosotres
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    baseline-on-migrate: true
