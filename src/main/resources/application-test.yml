spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dosotres
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    baseline-on-migrate: true
  mail:
    host: localhost
    port: 25
    password: ""

app:
  timer:
    cleanup-enabled: false
  goal:
    reminders-enabled: false
  reengagement:
    enabled: false
  admin:
    weekly-report:
      enabled: false
      cron: "0 0 8 * * MON"
      recipient: admin@test.com
