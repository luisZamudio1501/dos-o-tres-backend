spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dosotres
    username: dosotres_app
    password: dosotres123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    baseline-on-migrate: true

app:
  seed:
    enabled: true

logging:
  level:
    com.dosotres: DEBUG
    org.hibernate.SQL: DEBUG